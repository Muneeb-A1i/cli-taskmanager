# 26-09-2026

## Worked on

Fixing the errors and printing strings from user input.

## Investigated

I searched up the errors on google and came across forums from stack overflow which helped me.

some of the errors were:

- warning: format ‘%s’ expects argument of type ‘char *’, but argument 2 has type ‘int’ [-Wformat=]
    9 |         printf("Task Name: %s\n", task_name[30]);
      |                            ~^     ~~~~~~~~~~~~~
      |                             |              |
      |                             char *         int
      |                            %d

According to the stack overflow forums, the problem was in scanf, where i wrote "&task_name" i had to remove the &.

- error: array size missing in 'task_name'

according to the stack overflow forum, all i had to do was add a value in [].

and it sort of worked...

I was able to print a string but only one word due to whitespace.

so i investigated further, i watched a video on C from BroCode, a youtuber I like regarding programming.

to print multiple words, i had to use `fgets(task_name, sizeof(task_name), stdin);`

## Result

It finally worked!!!

I was able to enter a task name and get it to print.

## Next

Create a table to store tasks, and add function to remove tasks.
